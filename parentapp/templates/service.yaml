apiVersion: v1
kind: Service
metadata:
  name: {{ include "parentapp.fullname" . }}
  labels:
    {{- include "parentapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "parentapp.selectorLabels" . | nindent 4 }}
