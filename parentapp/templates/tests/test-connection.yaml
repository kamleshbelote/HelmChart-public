apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "parentapp.fullname" . }}-test-connection"
  labels:
    {{- include "parentapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "parentapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
